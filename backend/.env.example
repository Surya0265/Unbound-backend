# Environment Variables
DATABASE_URL="postgresql://postgres:password@localhost:5432/command_gateway"
PORT=3001

# Frontend URL (for email button links)
FRONTEND_URL=http://localhost:3000

# SMTP Email Configuration (for email notifications)
# Use Gmail, SendGrid, Mailgun, or any SMTP provider
# Leave blank to disable email notifications
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=Command Gateway <noreply@yourdomain.com>
